#! /usr/bin/env python

import argh

@arg('embeds_path', help='Path to pretrained embeddings in text format')
@arg('out_path', help='Path to out dir -- defaults to the same dir as input')
def compress(path_2_text_embeds, outpath):
   
@arg('lmqebin_path', help='Path to quantized binary file')
@arg('metadata_path', help='Path to metadata file')
@arg('out_path', help='Path for text file containing inflated embeddings')
def inflate():

@arg('wordlist_path')
@arg('out_path'
def list2trie():



# assembling:

parser = argh.ArghParser()
parser.add_commands([compress])

# dispatching:

if __name__ == '__main__':
    parser.dispatch()
